/**
 * The single source of truth for the ghost's refusal (SPEC §5). The evals
 * compare against this exact string, so it is defined once and imported
 * everywhere — never re-typed inline.
 */
export const CANONICAL_REFUSAL =
  "I cannot say. The evidence is silent on that, and I do not invent.";
